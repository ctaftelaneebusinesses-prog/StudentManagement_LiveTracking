export interface LiveTrip { id: string; vehicle_id: string; direction: string; status: string; started_at: string; last_latitude: number | null; last_longitude: number | null; last_location_at: string | null; vehicles?: { vehicle_number: string; routes?: { name: string; route_code: string } | null } | null; drivers?: { users: { full_name: string } | null } | null; }
export interface LiveLocation { vehicleId: string; latitude: number; longitude: number; label: string; updatedAt?: string | null; }
export interface ParentVehicleAssignment { students: { id: string; users: { full_name: string } | null }; pickup_points: { id: string; name: string; pickup_time: string | null; routes: { id: string; name: string; route_code: string; vehicles: { id: string; vehicle_number: string; trips: LiveTrip[] }[] } }; }

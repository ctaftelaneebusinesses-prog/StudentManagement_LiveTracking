export interface Driver { id: string; license_number: string; license_expiry: string | null; users: { full_name: string; email: string; phone: string | null; is_active: boolean }; }
export interface PickupPoint { id: string; name: string; address: string | null; stop_order: number; pickup_time: string | null; }
export interface Route { id: string; name: string; route_code: string; description: string | null; is_active: boolean; pickup_points: PickupPoint[]; }
export interface Vehicle { id: string; vehicle_number: string; capacity: number; driver_id: string | null; route_id: string | null; make_model: string | null; gps_device_id: string | null; is_active: boolean; drivers: { users: { full_name: string } } | null; routes: { name: string; route_code: string } | null; }
export interface DriverDashboard { vehicle: Vehicle | null; pickup_points: PickupPoint[]; students: { pickup_point_id: string; students: { id: string; admission_no: string; users: { full_name: string }; classes: { name: string; section: string } | null } }[]; trips: { id: string; trip_date: string; direction: string; status: string; last_latitude: number | null; last_longitude: number | null; last_location_at: string | null }[]; }
